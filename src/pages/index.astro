---
import Layout from '../layouts/Layout.astro';
import * as i18n from '../i18n/en.md';
import App from '../components/App.vue';
---

<Layout title={i18n.frontmatter.title}>
	<App
		client:only="vue"
		i18n={i18n.frontmatter}
		shareUrl={`https://localhost:4321`}
	/>
</Layout>
